name: lsp_client
description: Pure Dart LSP client — JSON-RPC 2.0 over stdio with Content-Length framing. Zero Flutter dependency.
version: 0.1.0
repository: https://github.com/daslaller/dart_lsp_client

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
